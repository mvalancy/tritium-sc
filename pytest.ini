[pytest]
testpaths = tests
markers =
    unit: Pure logic tests, no hardware or network required
    hardware: Requires BCC950 camera physically connected
    integration: Requires ML models (YOLO, Whisper, Ollama)
    slow: Tests that take more than 5 seconds
addopts = -v --tb=short
